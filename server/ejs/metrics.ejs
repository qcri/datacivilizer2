<!DOCTYPE html>
<%
    var metric;
    for (var metricNo in metrics) {
        metric = metrics[metricNo];
%>
    <a class="w3-button w3-block w3-left-align w3-small" onclick="switchSelect(this)" data-selected="false" data-type="metric" data-value="<%= metric %>"><%= metric %></a>
<%
    }
%>